server:
  port: 8080

spring:
  main:
    allow-bean-definition-overriding: true
  profiles:
    active: file
  flyway:
    enabled: false
  security:
    user:
      name: user
      password: password

invoicing-system:
  allowedOrigins: http://localhost:4200
  csrf:
    disable: true

logging:
  level:
    root: INFO
    pl:
      futurecollars:
        invoicing.db: DEBUG
